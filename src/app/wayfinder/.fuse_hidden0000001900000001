'use client';

import Link from 'next/link';
import { CTASection } from '@/components/CTASection';
import { useInView } from '@/hooks/useInView';

/* ------------------------------------------------------------------ */
/*  Section 1 - Hero                                                   */
/* ------------------------------------------------------------------ */

function DashboardMockup() {
  return (
    <div className="rounded-2xl border border-tt-gray-700 bg-tt-gray-900 p-4 sm:p-8">
      <div className="flex gap-4">
        {/* Sidebar */}
        <div className="hidden w-40 flex-shrink-0 space-y-3 sm:block">
          <div className="h-6 w-24 rounded bg-tt-teal/20" />
          <div className="space-y-2 pt-4">
            {['Dashboard', 'Campaigns', 'Analytics', 'Insights', 'Settings'].map(
              (item) => (
                <div
                  key={item}
                  className="flex items-center gap-2 rounded-md px-2 py-1.5 text-xs text-tt-gray-500"
                >
                  <span className="h-2 w-2 rounded-full bg-tt-gray-700" />
                  {item}
                </div>
              )
            )}
          </div>
        </div>

        {/* Main content */}
        <div className="flex-1 space-y-4">
          {/* Metric cards row */}
          <div className="grid grid-cols-3 gap-3">
            {[
              { label: 'ROAS', value: '4.2x', change: '+18%' },
              { label: 'CAC', value: '$23.40', change: '-12%' },
              { label: 'Conv. Rate', value: '3.8%', change: '+7%' },
            ].map((metric) => (
              <div
                key={metric.label}
                className="rounded-lg border border-tt-gray-700 bg-tt-black p-3"
              >
                <p className="text-[10px] uppercase tracking-wider text-tt-gray-500">
                  {metric.label}
                </p>
                <p className="mt-1 text-lg font-bold text-white">
                  {metric.value}
                </p>
                <p className="text-xs text-tt-teal">{metric.change}</p>
              </div>
            ))}
          </div>

          {/* Chart area */}
          <div className="rounded-lg border border-tt-gray-700 bg-tt-black p-4">
            <p className="text-[10px] uppercase tracking-wider text-tt-gray-500">
              Cross-Channel Performance
            </p>
            <div className="mt-4 flex items-end gap-1.5 h-24">
              {[40, 55, 45, 65, 50, 72, 60, 80, 68, 85, 75, 92].map(
                (h, i) => (
                  <div
                    key={i}
                    className="flex-1 rounded-t bg-tt-teal/70 transition-all duration-300"
                    style={{ height: `${h}%` }}
                  />
                )
              )}
            </div>
            <div className="mt-2 flex justify-between text-[9px] text-tt-gray-600">
              <span>Jan</span>
              <span>Jun</span>
              <span>Dec</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

function HeroSection() {
  return (
    <section className="relative min-h-[60vh] flex items-center bg-tt-black overflow-hidden">
      {/* Ambient glow */}
      <div className="pointer-events-none absolute inset-0" aria-hidden="true">
        <div className="absolute top-[-15%] left-[20%] h-[500px] w-[500px] rounded-full bg-tt-teal/[.08] blur-[120px]" />
        <div className="absolute bottom-[-10%] right-[10%] h-[400px] w-[400px] rounded-full bg-tt-orange/[.04] blur-[100px]" />
      </div>

      <div className="relative z-10 mx-auto max-w-7xl px-6 py-24">
        <div className="grid items-center gap-16 lg:grid-cols-2">
          {/* Text */}
          <div>
            <p className="text-sm font-semibold uppercase tracking-[4px] text-tt-teal">
              Proprietary Technology
            </p>

            <h1 className="mt-6 text-5xl font-extrabold leading-[1.08] tracking-tight md:text-7xl">
              <span className="inline-block bg-gradient-to-r from-tt-teal to-tt-teal-light bg-clip-text text-transparent">
                Wayfinder AI
              </span>
              <br />
              <span className="text-white">
                Marketing Intelligence That Thinks Ahead
              </span>
            </h1>

            <p className="mt-6 max-w-xl text-xl text-tt-gray-400">
              Connect your marketing stack. One platform for cross-channel
              insights, optimization, and decision-making.
            </p>

            <div className="mt-10">
              <Link
                href="/get-started"
                className="inline-block rounded-full bg-tt-orange px-8 py-4 text-lg font-semibold text-white transition-all duration-200 hover:bg-tt-orange-dark hover:shadow-xl hover:-translate-y-0.5 active:translate-y-0"
              >
                See Wayfinder in Action
              </Link>
            </div>
          </div>

          {/* Dashboard mockup */}
          <div className="hidden lg:block">
            <DashboardMockup />
          </div>
        </div>

        {/* Mobile mockup */}
        <div className="mt-12 lg:hidden">
          <DashboardMockup />
        </div>
      </div>
    </section>
  );
}

/* ------------------------------------------------------------------ */
/*  Section 2 -What It Does                                           */
/* ------------------------------------------------------------------ */

const capabilities = [
  {
    title: 'Cross-Platform Intelligence',
    description:
      'Unified view across Google Ads, GA4, Search Console, Meta, and beyond. No more toggling between platforms.',
  },
  {
    title: 'ML-Driven Recommendations',
    description:
      'Machine learning models surface optimization opportunities humans miss. Not gut feeling. Data.',
  },
  {
    title: 'Real-Time Optimization',
    description:
      'Speed matters. Wayfinder processes data continuously, delivering actionable insights when they matter most.',
  },
  {
    title: 'Built by Practitioners',
    description:
      'Created by the same team that built campaigns at Google. Every feature solves a real problem we encountered at scale.',
  },
] as const;

function CapabilitiesSection() {
  return (
    <section className="bg-white py-24 sm:py-32">
      <div className="mx-auto max-w-7xl px-6 lg:px-8">
        <div className="mx-auto max-w-2xl text-center">
          <h2 className="text-3xl font-bold tracking-tight text-tt-black sm:text-4xl">
            Built In-House. Built Different.
          </h2>
        </div>

        <div className="mx-auto mt-16 grid max-w-5xl grid-cols-1 gap-8 md:grid-cols-2">
          {capabilities.map((cap) => (
            <div
              key={cap.title}
              className="rounded-2xl bg-tt-gray-50 p-8"
            >
              <h3 className="flex items-center gap-3 text-xl font-bold text-tt-black">
                <span className="inline-block h-3 w-3 flex-shrink-0 rounded-full bg-tt-teal" />
                {cap.title}
              </h3>
              <p className="mt-4 text-sm leading-relaxed text-tt-gray-600">
                {cap.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

/* ------------------------------------------------------------------ */
/*  Section 3 -Why It Matters (Comparison)                            */
/* ------------------------------------------------------------------ */

const withoutItems = [
  'Siloed data across platforms',
  'Manual analysis and gut-feel decisions',
  'Slow reaction to performance shifts',
  'Generic dashboards that don\'t drive action',
] as const;

const withItems = [
  'Unified cross-platform intelligence',
  'ML-driven, data-backed recommendations',
  'Real-time alerts and optimization signals',
  'Actionable insights that compound performance',
] as const;

function ComparisonSection() {
  const { ref, inView } = useInView({ threshold: 0.2 });

  return (
    <section className="bg-tt-gray-900 py-24 sm:py-32">
      <div className="mx-auto max-w-7xl px-6 lg:px-8">
        <div className="mx-auto max-w-3xl text-center">
          <h2 className="text-3xl font-bold tracking-tight text-white sm:text-4xl">
            Most Agencies Use the Same Tools You Already Have. We Built Our Own.
          </h2>
        </div>

        <div
          ref={ref}
          className={`mx-auto mt-16 grid max-w-4xl grid-cols-1 gap-8 md:grid-cols-2 transition-all duration-700 ${
            inView ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-6'
          }`}
        >
          {/* Without */}
          <div className="rounded-2xl border border-tt-gray-700 bg-tt-black p-8">
            <h3 className="text-lg font-semibold text-tt-gray-400">
              Without Wayfinder
            </h3>
            <ul className="mt-6 space-y-4">
              {withoutItems.map((item) => (
                <li
                  key={item}
                  className="flex items-start gap-3 text-sm text-tt-gray-500"
                >
                  <svg
                    className="mt-0.5 h-4 w-4 flex-shrink-0 text-red-400"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                    strokeWidth={2}
                  >
                    <path
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      d="M6 18L18 6M6 6l12 12"
                    />
                  </svg>
                  {item}
                </li>
              ))}
            </ul>
          </div>

          {/* With */}
          <div className="rounded-2xl border border-tt-teal/30 bg-tt-black p-8">
            <h3 className="text-lg font-semibold text-white">
              With Wayfinder
            </h3>
            <ul className="mt-6 space-y-4">
              {withItems.map((item) => (
                <li
                  key={item}
                  className="flex items-start gap-3 text-sm text-white"
                >
                  <svg
                    className="mt-0.5 h-4 w-4 flex-shrink-0 text-tt-teal"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                    strokeWidth={2}
                  >
                    <path
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      d="M5 13l4 4L19 7"
                    />
                  </svg>
                  {item}
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>
  );
}

/* ------------------------------------------------------------------ */
/*  Section 4 -Feed Optimization Platform                             */
/* ------------------------------------------------------------------ */

const feedFeatures = [
  {
    title: 'AI-Powered Optimization',
    description:
      'ML algorithms optimize product titles, descriptions, and attributes for maximum visibility and conversion.',
  },
  {
    title: 'Real-Time Processing',
    description:
      'Process thousands of products instantly. No batch delays, no stale data.',
  },
  {
    title: 'Performance Analytics',
    description:
      'Quality scoring and ROI tracking for every product in your feed.',
  },
] as const;

function FeedSection() {
  return (
    <section className="bg-white py-24 sm:py-32">
      <div className="mx-auto max-w-7xl px-6 lg:px-8">
        <div className="mx-auto max-w-2xl text-center">
          <h2 className="text-3xl font-bold tracking-tight text-tt-black sm:text-4xl">
            Feed Optimization Platform
          </h2>
          <p className="mt-4 text-lg text-tt-gray-600">
            Transform your e-commerce product feeds with AI-powered optimization
            and comprehensive performance analytics.
          </p>
        </div>

        <div className="mx-auto mt-16 grid max-w-5xl grid-cols-1 gap-8 md:grid-cols-3">
          {feedFeatures.map((feature) => (
            <div
              key={feature.title}
              className="rounded-xl bg-tt-gray-50 p-6"
            >
              <h3 className="flex items-center gap-3 text-lg font-semibold text-tt-black">
                <span className="inline-block h-2.5 w-2.5 flex-shrink-0 rounded-full bg-tt-teal" />
                {feature.title}
              </h3>
              <p className="mt-3 text-sm leading-relaxed text-tt-gray-600">
                {feature.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

/* ------------------------------------------------------------------ */
/*  Page                                                               */
/* ------------------------------------------------------------------ */

export default function WayfinderPage() {
  return (
    <main>
      {/* 1 -Hero */}
      <HeroSection />

      {/* 2 -Capabilities */}
      <CapabilitiesSection />

      {/* 3 -Comparison */}
      <ComparisonSection />

      {/* 4 -Feed Optimization Platform */}
      <FeedSection />

      {/* 5 -CTA */}
      <CTASection
        headline="See What Wayfinder Can Do For You"
        subheadline="Book a demo and see how proprietary AI changes the game for performance marketing."
        primaryCTA={{ text: 'Book Your Free Audit', href: '/get-started' }}
        dark
        badges={[
          'Proprietary AI',
          'Built by Ex-Google',
          'Cross-Platform Intelligence',
        ]}
      />
    </main>
  );
}
